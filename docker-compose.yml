services:
  sqk-site:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sqk-site
    ports:
      - "15000:3000"
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3000
